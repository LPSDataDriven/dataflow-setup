name: Test CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:  # Permite execução manual

env:
  AWS_REGION: us-east-1
  S3_BUCKET: lpsdata-airflow-1

jobs:
  # Job 1: Validação básica
  basic-validation:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Validate project structure
      run: |
        echo "🔍 Verificando estrutura do projeto..."

        # Verificar se os diretórios principais existem
        if [ -d "dbt" ]; then
          echo "✅ Diretório dbt encontrado"
        else
          echo "❌ Diretório dbt não encontrado"
          exit 1
        fi

        if [ -d "airflow-local" ]; then
          echo "✅ Diretório airflow-local encontrado"
        else
          echo "❌ Diretório airflow-local não encontrado"
          exit 1
        fi

        if [ -f ".dbt/profiles.yml" ]; then
          echo "✅ Arquivo profiles.yml encontrado"
        else
          echo "❌ Arquivo profiles.yml não encontrado"
          exit 1
        fi

        echo "🎉 Estrutura do projeto validada!"

    - name: Test Python syntax
      run: |
        echo "🔍 Testando sintaxe Python..."

        # Verificar se há arquivos Python para testar
        if find . -name "*.py" -not -path "./.venv/*" -not -path "./.git/*" | head -1 | grep -q .; then
          echo "📁 Arquivos Python encontrados, testando sintaxe..."
          find . -name "*.py" -not -path "./.venv/*" -not -path "./.git/*" -exec python -m py_compile {} \;
          echo "✅ Sintaxe Python validada!"
        else
          echo "ℹ️ Nenhum arquivo Python encontrado para testar"
        fi

    - name: Test DBT project structure
      run: |
        echo "🔍 Verificando estrutura do projeto DBT..."

        if [ -f "dbt/dbt_project.yml" ]; then
          echo "✅ dbt_project.yml encontrado"
        else
          echo "❌ dbt_project.yml não encontrado"
          exit 1
        fi

        if [ -d "dbt/models" ]; then
          echo "✅ Diretório models encontrado"
        else
          echo "ℹ️ Diretório models não encontrado (criando estrutura básica...)"
          mkdir -p dbt/models
          echo "✅ Diretório models criado"
        fi

        echo "🎉 Estrutura DBT validada!"

  # Job 2: Teste de conectividade (apenas se secrets estiverem configurados)
  connectivity-test:
    runs-on: ubuntu-latest
    if: ${{ secrets.AWS_ACCESS_KEY_ID != '' }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Test AWS connectivity
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_DEFAULT_REGION: ${{ env.AWS_REGION }}
      run: |
        echo "🔍 Testando conectividade AWS..."

        # Usar AWS CLI que já vem instalado no runner
        aws sts get-caller-identity

        echo "✅ Conectividade AWS funcionando!"

        # Testar acesso ao S3 (sem criar bucket se não existir)
        if aws s3 ls "s3://${{ env.S3_BUCKET }}" 2>/dev/null; then
          echo "✅ Bucket S3 acessível: ${{ env.S3_BUCKET }}"
        else
          echo "ℹ️ Bucket S3 não encontrado: ${{ env.S3_BUCKET }}"
          echo "💡 Isso é normal se o bucket ainda não foi criado"
        fi

  # Job 3: Teste de DBT (apenas se secrets do Snowflake estiverem configurados)
  dbt-test:
    runs-on: ubuntu-latest
    if: ${{ secrets.SNOWFLAKE_ACCOUNT != '' }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install DBT
      run: |
        pip install --upgrade pip
        pip install dbt-core dbt-snowflake

    - name: Test DBT connection
      env:
        SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
        SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
        SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
        SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
        SNOWFLAKE_DATABASE_DEV: ${{ secrets.SNOWFLAKE_DATABASE_DEV }}
        SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
        SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
      run: |
        echo "🔍 Testando conexão DBT com Snowflake..."

        # Validar sintaxe do projeto
        cd dbt
        dbt parse --profiles-dir ../.dbt

        # Testar conexão (sem executar)
        dbt debug --profiles-dir ../.dbt

        echo "✅ Conexão DBT funcionando!"

  # Job 4: Notificação de sucesso
  success-notification:
    needs: [basic-validation]
    runs-on: ubuntu-latest
    if: always()
    steps:
    - name: Success notification
      run: |
        echo "🎉 Pipeline de validação básica concluído!"
        echo ""
        echo "📋 Status dos jobs:"
        echo "✅ Validação básica: ${{ needs.basic-validation.result }}"
        echo "🔗 Teste de conectividade: ${{ needs.connectivity-test.result || 'Pulado (secrets não configurados)' }}"
        echo "🔗 Teste DBT: ${{ needs.dbt-test.result || 'Pulado (secrets não configurados)' }}"
        echo ""
        echo "💡 Para habilitar todos os testes, configure os secrets no GitHub:"
        echo "   - AWS_ACCESS_KEY_ID"
        echo "   - AWS_SECRET_ACCESS_KEY"
        echo "   - SNOWFLAKE_ACCOUNT"
        echo "   - SNOWFLAKE_USER"
        echo "   - SNOWFLAKE_PASSWORD"
        echo "   - SNOWFLAKE_WAREHOUSE"
        echo "   - SNOWFLAKE_DATABASE_DEV"
        echo "   - SNOWFLAKE_SCHEMA"
        echo "   - SNOWFLAKE_ROLE"

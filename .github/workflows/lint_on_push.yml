name: lint dbt project on PR

on: [pull_request]

jobs:
  # this job runs SQLFluff with a specific set of rules
  lint_project:
    name: Run SQLFluff linter
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: "actions/checkout@v3"
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: "actions/setup-python@v4"
        with:
          python-version: "3.11"

      - name: Install SQLFluff and dependencies
        run: |
          python -m pip install sqlfluff
          python -m pip install dbt-core dbt-snowflake
          python -m pip install sqlfluff-templater-dbt

      - name: Lint SQL files with SQLFluff
        run: |
          echo "🔍 Executando SQLFluff com regras do .sqlfluff..."

          # Lint apenas arquivos SQL sem tentar conectar ao banco
          sqlfluff lint dbt/ --dialect snowflake --templater jinja

          echo "✅ SQLFluff executado com sucesso!"
